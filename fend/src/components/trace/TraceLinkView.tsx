import React from "react";
import styled from "styled-components";

export default function TraceLinkView(props: TraceLinkViewProps) {
  return <TraceLinkContainer>...this is page 4...</TraceLinkContainer>;
}

const TraceLinkContainer = styled.div``;
