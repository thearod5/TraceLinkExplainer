"""
A DistanceMatrix is a multi-dimensional matrix
containing similarity scores calculated via cosine_similarity.
"""

"""
A TermFrequencyMatrix is a  multi-dimensional matrix
containing as rows a set of textual documents and
as columns each word appearing in the aggregate vocabulary of the documents.
Each entry in the matrix is a number meant to represent how much
"weight" a given column (word) has in each row (body doc).
"""
